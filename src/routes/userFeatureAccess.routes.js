// import express from "express";
// import {
//     setUserFeatureAccess,
//     blockUserFeature,
// } from "../controllers/userFeatureAccess.controller.js";
// import { authMiddleware } from "../middlewares/auth.middleware.js";
// import { adminOrSubAdminOnly } from "../middlewares/role.middleware.js";

// const router = express.Router();

// router.post("/", authMiddleware, adminOrSubAdminOnly, setUserFeatureAccess);
// router.patch("/:id/block", authMiddleware, adminOrSubAdminOnly, blockUserFeature);

// export default router;
