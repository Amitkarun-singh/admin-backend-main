// import express from "express";
// import {
//     setRoleFeatureAccess,
//     getRoleFeatureAccess,
// } from "../controllers/featureAccess.controller.js";
// import { authMiddleware } from "../middlewares/auth.middleware.js";
// import { adminOrSubAdminOnly } from "../middlewares/role.middleware.js";

// const router = express.Router();

// router.post("/", authMiddleware, adminOrSubAdminOnly, setRoleFeatureAccess);
// router.get("/:role", authMiddleware, adminOrSubAdminOnly, getRoleFeatureAccess);

// export default router;
