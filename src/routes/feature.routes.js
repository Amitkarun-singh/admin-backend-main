// import express from "express";
// import {
//     createFeature,
//     getAllFeatures,
//     toggleFeature,
// } from "../controllers/feature.controller.js";
// import { authMiddleware } from "../middlewares/auth.middleware.js";
// import { adminOrSubAdminOnly } from "../middlewares/role.middleware.js";

// const router = express.Router();

// router.post("/", authMiddleware, adminOrSubAdminOnly, createFeature);
// router.get("/", authMiddleware, adminOrSubAdminOnly, getAllFeatures);
// router.patch("/:id", authMiddleware, adminOrSubAdminOnly, toggleFeature);

// export default router;
